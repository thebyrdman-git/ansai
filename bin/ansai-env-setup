#!/bin/bash
set -e

# Resolve the repository root directory
REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

echo "Setting up ANSAI environment from: $REPO_ROOT"
ansible-playbook "$REPO_ROOT/orchestrators/ansible/playbooks/setup-local-shell.yml"

